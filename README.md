# مدرسة السليمانية

نظام إدارة أكاديمي (React + Vite + Firebase).

## التطوير المحلي

```bash
npm install
cp .env.example .env.local
npm run dev
```

## البناء

```bash
npm run build
```

المخرجات في `dist/` — مناسب لـ Vercel (Framework Preset: Vite) أو Firebase Hosting.
